name: Release

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    uses: glitter-gim/.github/.github/workflows/release-please.yml@v1

